import styled from "styled-components";

export default styled.div`
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    padding: 16px 0;
    background: var(--main-color-1);
    z-index: 10;
`;
